[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Vite builds a single-page app, so every path must fall back to index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
