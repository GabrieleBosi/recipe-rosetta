# Frontend environment. Copy to .env.local and fill in.
#
# Both values are safe in the browser: the publishable key can only do what row
# level security allows. GEMINI_API_KEY is NOT here on purpose. It lives as a
# Supabase secret, and only the Edge Function reads it.

VITE_SUPABASE_URL=https://pwruhqpefkbbrkgmgehn.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ZVRZPwh4MyOGZw52oW0PQQ_h4tpQjTB
